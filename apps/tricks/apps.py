from django.apps import AppConfig


class TricksConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.tricks"
    label = "tricks"
    verbose_name = "Tricks to Sound Fluent"
