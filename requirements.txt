# Diction Masters — everything the site needs to run.
#   pip install -r requirements.txt

Django>=5.2,<6.0
python-dotenv>=1.0          # reads .env in development
Pillow>=10.0                # image fields: video thumbnails, lesson images

# Uploads live on Cloudflare R2
boto3>=1.34
django-storages>=1.14

# Production
psycopg2-binary>=2.9        # PostgreSQL driver
gunicorn>=21.2              # the application server
whitenoise>=6.6             # serves the site's own CSS, JS and images
